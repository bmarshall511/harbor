# Database Rules
- Prisma schema must model typed core entities plus flexible metadata.
- Postgres is the primary production target.
- Track AI usage, audit logs, jobs, permissions, provider identities, and entity relations explicitly.
- Avoid collapsing everything into an untyped key/value store.
