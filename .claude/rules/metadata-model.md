# Metadata Model Rules
- Support metadata at both file and folder/event level.
- Use typed first-class fields where they materially improve UX, filtering, and sorting.
- Use flexible metadata only as an extension layer.
- Prioritize autocomplete, quick entry, and smart defaults.
